@model IEnumerable<Web_Page.Models.Tranferencia>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Tipocuenta)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.MontoR)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.fecha)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Cuenta.CodigCuenta)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Tipocuenta)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.MontoR)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.fecha)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Cuenta.CodigCuenta)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.CodTranf }) |
                @Html.ActionLink("Details", "Details", new { id=item.CodTranf }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.CodTranf })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
